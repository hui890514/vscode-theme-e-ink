import { readFileSync } from 'node:fs'
import { getThemeColors } from '../src/theme'

function getLocalRules() {
  // const file = readFileSync('../src/theme.ts', 'utf8')
  // const localRules = getThemeColors()
  // console.log(localRules)
}

getLocalRules()
